---
server:
  http2:
    tls:
      key: 'server.key'
      cert: 'server.crt'

vserver:
  default:
    root: /tmp/default
    log: &default_log
      file: /tmp/log

  alobbs.com:
    root: /tmp/alobbs
    log: *default_log
